import React from 'react';

    const ViewData = (props) => (

    <div className="card" style={{'width': '100%', 'marginTop': '10px'}}>
    <div className="card-body">
    {/* <h5 className="card-title">{props.pages.pageName}</h5>
    <h6 className="card-subtitle mb-2 text-muted">by {props.pages.url}</h6>
    <p className="card-text">{props.pages.fullUrl}</p>
    <a href={props.pages.typeLayout} className="card-link">Go to course ...</a> */}
    </div>
    </div>
);
export default ViewData;